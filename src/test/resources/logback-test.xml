<?xml version="1.0" encoding="UTF-8"?>

<configuration>
    <!-- Path to data logs -->
    <property name="LOG_PATH" value="${user.dir}/log/test" />

    <!-- Name of logs files -->
    <property name="LOG_NAME" value="test.log" />

    <!-- Pattern name of logs files -->
    <property name="LOG_PATTERN_NAME" value="test.%d{yyyy-MM-dd}.%i.log" />

    <!-- Logback log to file and to console -->
    <!-- Console appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_NAME}</file>
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%date %level [%thread] %logger{10} [%file:%line] %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- This log file will be archived daily or the file size is larger than 10MB -->
            <fileNamePattern>${LOG_PATH}/archived/${LOG_PATTERN_NAME}</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
        </rollingPolicy>
    </appender>

    <!-- Root logger -->
    <root level="info">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
